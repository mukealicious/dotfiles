{
  "id": "9b555dee",
  "title": "Validate Hammerspoon–Moja Glava integration end to end",
  "tags": [
    "hammerspoon",
    "moja-glava",
    "validation"
  ],
  "status": "closed",
  "created_at": "2026-08-12T18:42:00.854Z"
}

Run controlled real-world scenarios from Hammerspoon trigger through capture packet, queue/Herdr/Pi/Hunk, and final vault artifacts. Evaluate Save, queue serialization + promotion, Study-with-me read-only handoff, provenance/security, daily ledger, and holistic failure/observability behavior. Preserve pre-existing dotfiles/vault changes; do not commit or close user-owned panes.

## Handoff to Codex computer use (2026-08-12)
Copied a comprehensive remaining-test protocol to the clipboard and saved it at `/tmp/moja-glava-remaining-validation-prompt.md` (SHA-256 `c3b1fc63c47a89730b539639446e8fe9a85c020402ec35aa62e4c9fcaec1cea1`). It covers Study-with-me read-only/write phases, multi-item FIFO+bypass, promotion dedupe, TextEdit selection/clipboard, launch failure retention, reload loss/recovery, daily audit, full regression, and holistic reporting. Current queue is idle; last successful FIFO evidence is documented in the canonical study note.

Refined the shared Herdr skill after Codex Desktop was blocked outside Herdr. `HERDR_ENV=1` now means caller-relative identity is available; when unset, explicitly requested/delegated out-of-band control is allowed using narrowly discovered explicit IDs, never `--current` or UI focus. Reprojected via `ai/install.sh`. Updated and recopied the handoff prompt with explicit out-of-band authorization; new SHA-256 `68d2b2362e4a011eb9f82034441f51c69316fe5e719a1806bb54f6eba57885d1`.

## Readiness follow-up (2026-08-12)

Independent review found three pre-ship invariant breaks: URL privacy gaps for root/query URLs, fail-open queue release on ambiguous launcher/waiter failures, and a redundant Pi-visible `queue-agent.json` side channel. Additional readiness work: make packet close failures fatal, remove the Study-with-me prompt contradiction, gate Lua-test Hammerspoon restarts on an idle queue, align daily-note guidance, and add live runtime/permission doctor checks.

Approved implementation direction for supervised personal use: keep queue admission in memory, but fail closed on ambiguity. Do not build durable automatic replay yet. Retained capture packets remain the recovery record; accidental reload/crash requires Herdr inspection and explicit manual recovery. Preserve `--approve` because it trusts vault-local project guidance rather than auto-approving tools; document untrusted-content risk as a residual limitation.

## Readiness fixes completed (2026-08-12)

Implemented and validated the fail-closed supervised-use design:

- browser URL authority/query parsing now strips root-level queries/fragments correctly and retains validated `v`, `list`, and `t` only on exact YouTube hosts/subdomains;
- context persistence requires successful write, close/flush, and atomic rename;
- removed the `queue-agent.json` side channel and `MOJA_GLAVA_QUEUE_FILE` inheritance; AgentLauncher consumes the launcher's direct `pi_pane_id` receipt;
- launcher failure, invalid receipt, waiter creation/failure, and Pi `blocked` state now pause admission without starting the next writer;
- added explicit queue status and confirmed-safe manual resume APIs plus visible pause notification;
- Study-with-me's prompt and studying guidance are consistently read-only before participation;
- Lua fixtures atomically acquire Hammerspoon maintenance mode only while idle; new captures are blocked until restart;
- launcher and doctor preflight `pi-personal --version`; doctor now checks live IPC, loaded palette, permissions, and queue state;
- daily-note checkpoint guidance now matches the minimal `Captured Today` contract;
- documented reload/manual reconciliation and residual prompt-injection/user-managed concurrency limits.

Validation evidence: `/tmp/moja-glava-e2e-remaining-20260812-140213/09-readiness-follow-up/`. Passed ShellCheck, launcher/waiter/runner tests, 25/25 waiter stress, both live Hammerspoon Lua fixtures, Lua parsing, skill validation, both repository diff checks, source/install/projection equality, packet permissions/stale-file audit, and a live fail-closed two-request probe. Final oracle verdict: ready for supervised personal use with no remaining in-scope blocker. `dot-doctor`: Hammerspoon section fully passes; overall 218 passed, one unrelated `ni` PATH failure, one unrelated QMD PATH warning.

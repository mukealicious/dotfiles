{
  "name": "dotfiles-pi-config",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "npm install --package-lock=false --ignore-scripts >/dev/null && tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@types/node": "*",
    "typescript": "*"
  }
}

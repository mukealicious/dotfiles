# Bun aliases

alias b 'bun'
alias br 'bun run'
alias bi 'bun install'
alias ba 'bun add'
alias bad 'bun add -d'
alias brm 'bun remove'
alias bu 'bun update'
alias bx 'bunx'

# Scripts
alias bd 'bun dev'
alias bs 'bun start'
alias bt 'bun test'
alias btw 'bun test --watch'
alias bb 'bun build'

# Utilities
alias bc 'bun create'
alias binit 'bun init'
